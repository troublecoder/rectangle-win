# Rectangle Win — 프론트엔드 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Nuxt UI v4 설정은 nuxt-ui 스킬과 MCP를 참조.

**Goal:** Vue 3 + Nuxt UI **v4** + vue-konva + Pinia + Zod로 Tauri 데스크톱 앱 프론트엔드를 구축한다.

**Architecture:** Vue SPA (Nuxt 프레임워크 아님). Vite + `@nuxt/ui/vite` 플러그인. FSD 경량화 구조. Pinia로 전역 config store, Zod로 Tauri IPC 응답 런타임 검증.

**Tech Stack:** Vue 3, Vite 6, **@nuxt/ui ^4** (Vue SPA 모드: `@nuxt/ui/vite` + `@nuxt/ui/vue-plugin`), Pinia, Zod, vue-konva, @tauri-apps/api, tailwindcss ^4.

---

## 파일 구조

```
src/                             # 프론트엔드 루트 (Tauri frontendDist)
├── index.html                   # <div id="app" class="isolate">
├── main.ts                      # Vue 앱 진입점 (router + pinia + ui plugin)
├── App.vue                      # <UApp> 루트
├── assets/css/main.css          # @import "tailwindcss"; @import "@nuxt/ui";
├── vite-env.d.ts
│
├── entities/                    # 순수 TS 타입 + Zod 스키마
│   ├── config.ts                # Config Zod 스키마 + 타입
│   └── snap-target.ts           # SnapTarget Zod 스키마 + 타입
│
├── features/                    # composables + stores + API
│   ├── config/
│   │   ├── api.ts               # Tauri invoke 래퍼 (Zod 검증 적용)
│   │   └── stores.ts            # Pinia store (useConfigStore)
│   └── editor/
│       └── useCanvasEditor.ts   # vue-konva 에디터 composable
│
├── pages/                       # 사이드바 카테고리 페이지
│   ├── General.vue
│   ├── Throw.vue
│   ├── SnapEditor.vue
│   ├── Keyboard.vue
│   ├── Display.vue
│   └── About.vue
│
└── components/
    ├── SettingsLayout.vue       # 사이드바 + 라우터뷰
    ├── PropertyPanel.vue        # snap 영역 속성 폼
    └── ChainEditor.vue          # 체인 순서 편집
```

---

## Task 1: Vue + Vite + Nuxt UI v4 + Pinia + Zod 프로젝트 세팅

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`
- Create: `src/main.ts`, `src/App.vue`, `src/assets/css/main.css`, `src/vite-env.d.ts`
- Replace: `src/index.html`

- [ ] **Step 1: `package.json`**

```json
{
  "name": "rectangle-win-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5",
    "vue-router": "^4",
    "@nuxt/ui": "^4",
    "pinia": "^2",
    "zod": "^3",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-log": "^2",
    "vue-konva": "^3",
    "konva": "^9"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5",
    "vue-tsc": "^2",
    "typescript": "^5",
    "tailwindcss": "^4",
    "vite": "^6"
  }
}
```

- [ ] **Step 2: `vite.config.ts`** — Nuxt UI v4 Vue SPA 모드

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import ui from '@nuxt/ui/vite'
import { resolve } from 'path'

export default defineConfig({
  plugins: [
    vue(),
    ui({
      ui: {
        colors: {
          primary: 'blue',
          neutral: 'slate',
        },
      },
    }),
  ],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },
  clearScreen: false,
  server: {
    port: 3000,
    strictPort: true,
  },
  envPrefix: ['VITE_', 'TAURI_'],
  build: {
    target: 'es2021',
    outDir: 'dist',
  },
})
```

> **주의**: v4에서는 `@tailwindcss/vite`를 따로 쓰지 않음. `@nuxt/ui/vite`가 Tailwind를 통합 처리.

- [ ] **Step 3: `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2021",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "jsx": "preserve",
    "sourceMap": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "lib": ["ES2021", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "noEmit": true,
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["src/**/*.ts", "src/**/*.vue", "src/**/*.d.ts"]
}
```

- [ ] **Step 4: `src/assets/css/main.css`**

```css
@import "tailwindcss";
@import "@nuxt/ui";
```

- [ ] **Step 5: `src/vite-env.d.ts`**

```typescript
/// <reference types="vite/client" />

declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  const component: DefineComponent<{}, {}, any>
  export default component
}
```

- [ ] **Step 6: `src/index.html`** 교체

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Rectangle Win</title>
</head>
<body>
  <div id="app" class="isolate"></div>
  <script type="module" src="/src/main.ts"></script>
</body>
</html>
```

> `class="isolate"`는 Nuxt UI Vue 모드 필수 (스킬 가이드라인).

- [ ] **Step 7: `src/main.ts`** — Pinia + Router + Nuxt UI vue-plugin

```typescript
import './assets/css/main.css'
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import { createRouter, createWebHashHistory } from 'vue-router'
import ui from '@nuxt/ui/vue-plugin'
import App from './App.vue'

const app = createApp(App)

const router = createRouter({
  history: createWebHashHistory(),
  routes: [
    { path: '/', redirect: '/general' },
    { path: '/general', name: 'general', component: () => import('./pages/General.vue') },
    { path: '/throw', name: 'throw', component: () => import('./pages/Throw.vue') },
    { path: '/snap-editor', name: 'snap-editor', component: () => import('./pages/SnapEditor.vue') },
    { path: '/keyboard', name: 'keyboard', component: () => import('./pages/Keyboard.vue') },
    { path: '/display', name: 'display', component: () => import('./pages/Display.vue') },
    { path: '/about', name: 'about', component: () => import('./pages/About.vue') },
  ],
})

app.use(createPinia())
app.use(router)
app.use(ui)
app.mount('#app')
```

- [ ] **Step 8: `src/App.vue`** — UApp 래퍼

```vue
<script setup lang="ts">
import SettingsLayout from '@/components/SettingsLayout.vue'
</script>

<template>
  <UApp>
    <SettingsLayout />
  </UApp>
</template>
```

- [ ] **Step 9: `src/components/SettingsLayout.vue`** — 사이드바

Nuxt UI v4 `UNavigationMenu` vertical. semantic colors 사용.

```vue
<script setup lang="ts">
import type { NavigationMenuItem } from '@nuxt/ui'

const items = computed<NavigationMenuItem[][]>([
  [
    { label: 'Settings', type: 'label' },
    { label: 'General', icon: 'i-lucide-settings', to: '/general' },
    { label: 'Throw', icon: 'i-lucide-mouse-pointer-click', to: '/throw' },
    { label: 'Snap Editor', icon: 'i-lucide-layout-grid', to: '/snap-editor' },
    { label: 'Keyboard', icon: 'i-lucide-keyboard', to: '/keyboard' },
    { label: 'Display', icon: 'i-lucide-monitor', to: '/display' },
    { label: 'About', icon: 'i-lucide-info', to: '/about' },
  ],
  [
    { label: 'Pause', icon: 'i-lucide-pause' },
    { label: 'Quit', icon: 'i-lucide-log-out' },
  ],
])
</script>

<template>
  <div class="flex h-screen w-screen overflow-hidden bg-default text-default">
    <aside class="w-56 shrink-0 border-r border-default flex flex-col">
      <div class="p-4 border-b border-default">
        <h1 class="text-lg font-bold flex items-center gap-2">
          <span class="inline-block size-5 rounded bg-primary"></span>
          Rectangle Win
        </h1>
      </div>
      <UNavigationMenu
        orientation="vertical"
        :items="items"
        highlight
        class="flex-1 data-[orientation=vertical]:w-full border-0"
      />
    </aside>
    <main class="flex-1 overflow-auto p-6">
      <RouterView />
    </main>
  </div>
</template>
```

- [ ] **Step 10: 임시 페이지 6개 생성**

`src/pages/` 디렉토리에 General.vue, Throw.vue, SnapEditor.vue, Keyboard.vue, Display.vue, About.vue. 각각 제목+설명만.

- [ ] **Step 11: npm install + build 확인**

Run: `npm install && npm run build`
Expected: 빌드 성공

- [ ] **Step 12: tauri.conf.json 업데이트**

```json
"build": {
  "frontendDist": "../src/dist",
  "devUrl": "http://localhost:3000",
  "beforeDevCommand": "npm run dev",
  "beforeBuildCommand": "npm run build"
}
```

- [ ] **Step 13: 커밋**

```bash
git add -A
git commit -m "feat: Vue 3 + Nuxt UI v4 + Pinia + Zod 프론트엔드 세팅 및 사이드바"
```

---

## Task 2: entities (Zod 스키마) + config API + Pinia store

**Files:**
- Create: `src/entities/snap-target.ts`
- Create: `src/entities/config.ts`
- Create: `src/features/config/api.ts`
- Create: `src/features/config/stores.ts`

- [ ] **Step 1: `src/entities/snap-target.ts`** — Zod 스키마

```typescript
import { z } from 'zod'

export const windowActionSchema = z.enum([
  'Maximize', 'Minimize', 'Restore', 'Center',
  'AlmostMaximize', 'MaximizeHeight',
  'NextDisplay', 'PreviousDisplay',
])
export type WindowAction = z.infer<typeof windowActionSchema>

export const snapTargetAreaSchema = z.object({
  kind: z.literal('area'),
  id: z.string(),
  name: z.string(),
  x_ratio: z.number().min(0).max(1),
  y_ratio: z.number().min(0).max(1),
  w_ratio: z.number().min(0).max(1),
  h_ratio: z.number().min(0).max(1),
})

export const snapTargetActionSchema = z.object({
  kind: z.literal('action'),
  id: z.string(),
  name: z.string(),
  action: windowActionSchema,
})

export const snapTargetSchema = z.discriminatedUnion('kind', [
  snapTargetAreaSchema,
  snapTargetActionSchema,
])
export type SnapTarget = z.infer<typeof snapTargetSchema>
```

- [ ] **Step 2: `src/entities/config.ts`** — Config Zod 스키마

```typescript
import { z } from 'zod'
import { snapTargetSchema } from './snap-target'

export const modifierModeSchema = z.enum(['Shared', 'Separate', 'OverrideOs'])
export type ModifierMode = z.infer<typeof modifierModeSchema>

export const configSchema = z.object({
  general: z.object({
    launch_at_login: z.boolean(),
    start_minimized: z.boolean(),
    show_in_tray: z.boolean(),
    language: z.string(),
  }),
  snap: z.object({
    active_preset: z.string(),
    areas: z.array(snapTargetSchema),
  }),
  throw: z.object({
    trigger_modifiers: z.array(z.string()),
    long_throw_enabled: z.boolean(),
    long_throw_distance: z.number(),
    mapping: z.record(z.string(), z.string()),
    long_throw_mapping: z.record(z.string(), z.string()),
  }),
  keyboard: z.object({
    enabled: z.boolean(),
    trigger_modifiers: z.array(z.string()),
    modifier_mode: modifierModeSchema,
    cycle_timeout_ms: z.number(),
    chains: z.object({
      horizontal: z.array(z.string()),
      vertical: z.array(z.string()),
    }),
  }),
  overlay: z.object({
    reticle_style: z.string(),
    cursor_indicator: z.boolean(),
    cursor_radius: z.number(),
    cursor_color: z.string(),
    cursor_opacity: z.number(),
    sector_highlight_color: z.string(),
    sector_count: z.number(),
    snap_preview: z.boolean(),
  }),
  update: z.object({
    enabled: z.boolean(),
    channel: z.string(),
    check_on_startup: z.boolean(),
  }),
})

export type Config = z.infer<typeof configSchema>
```

- [ ] **Step 3: `src/features/config/api.ts`** — Tauri invoke + Zod 검증

```typescript
import { invoke } from '@tauri-apps/api/core'
import { configSchema, type Config } from '@/entities/config'

export async function getConfig(): Promise<Config> {
  const raw = await invoke('get_config')
  return configSchema.parse(raw)  // 런타임 검증
}

export async function saveConfig(config: Config): Promise<void> {
  await invoke('save_config', { config })
}

export async function getConfigPath(): Promise<string> {
  return invoke<string>('get_config_path')
}

export async function applyPreset(presetName: string): Promise<Config> {
  const raw = await invoke('apply_preset', { presetName })
  return configSchema.parse(raw)
}

export interface MonitorInfo {
  x: number
  y: number
  width: number
  height: number
}

export async function getMonitors(): Promise<MonitorInfo[]> {
  return invoke<MonitorInfo[]>('get_monitors')
}

export async function testSnapToSector(sector: number, cursorX: number, cursorY: number): Promise<void> {
  await invoke('test_snap_to_sector', { sector, cursorX, cursorY })
}
```

- [ ] **Step 4: `src/features/config/stores.ts`** — Pinia store

```typescript
import { defineStore } from 'pinia'
import { ref, watch } from 'vue'
import type { Config } from '@/entities/config'
import { getConfig, saveConfig } from './api'

export const useConfigStore = defineStore('config', () => {
  const config = ref<Config | null>(null)
  const loading = ref(false)
  const saving = ref(false)

  let saveTimer: ReturnType<typeof setTimeout> | null = null

  async function load() {
    loading.value = true
    try {
      config.value = await getConfig()
    } finally {
      loading.value = false
    }
  }

  async function save() {
    if (!config.value) return
    saving.value = true
    try {
      await saveConfig(config.value)
    } finally {
      saving.value = false
    }
  }

  // debounce 자동 저장 (300ms)
  watch(config, () => {
    if (saveTimer) clearTimeout(saveTimer)
    saveTimer = setTimeout(() => save(), 300)
  }, { deep: true })

  return { config, loading, saving, load, save }
})
```

- [ ] **Step 5: 빌드 확인**

Run: `npm run build`
Expected: TypeScript 컴파일 성공

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "feat: Zod 스키마 entities + Tauri API (검증) + Pinia config store"
```

---

## Task 3: 설정 페이지들 (General, Throw, Keyboard, Display, About)

각 페이지는 Pinia store에서 config를 읽고, Nuxt UI 컴포넌트로 폼 구성. `UFormField` + semantic colors.

**Files:**
- Replace: `src/pages/General.vue`, `Throw.vue`, `Keyboard.vue`, `Display.vue`, `About.vue`

- [ ] **Step 1: General.vue** — USwitch 3개 + USelect

`useConfigStore()`에서 config 로드 (onMounted). launch_at_login, start_minimized, show_in_tray 스위치. language 셀렉트.

- [ ] **Step 2: Throw.vue** — trigger_modifiers, long throw 설정

UInput (trigger_modifiers, 쉼표 구분), USwitch (long_throw_enabled), UInputNumber (long_throw_distance).

- [ ] **Step 3: Keyboard.vue** — URadioGroup (3모드) + override 경고 + ChainEditor

modifier_mode 라디오 (Separate/Shared/OverrideOs). OverrideOs 선택시 UAlert 경고 표시 (v-if). trigger_modifiers는 Separate/Shared만 표시. cycle_timeout_ms. 하단 ChainEditor.

- [ ] **Step 4: Display.vue** — reticle/overlay 시각 설정

USelect (reticle_style: pie/ring/minimal), USwitch (cursor_indicator, snap_preview), UInputNumber (cursor_radius, cursor_opacity), UInput type=color (cursor_color, sector_highlight_color), USelect (sector_count: 4/8/12).

- [ ] **Step 5: About.vue** — 버전 + 업데이트

버전 표시. USwitch (update.enabled, update.check_on_startup). USelect (update.channel: stable/beta). UButton "업데이트 확인" (tauri-plugin-updater API).

- [ ] **Step 6: 빌드 + 커밋**

```bash
npm run build
git add -A
git commit -m "feat: 5개 설정 페이지 구현 (Pinia store + Nuxt UI v4)"
```

---

## Task 4: Snap Editor (vue-konva 3패널 에디터)

**Files:**
- Replace: `src/pages/SnapEditor.vue`
- Create: `src/components/PropertyPanel.vue`
- Create: `src/components/ChainEditor.vue`
- Create: `src/features/editor/useCanvasEditor.ts`

- [ ] **Step 1: `useCanvasEditor.ts`** — Pinia store 기반 에디터 composable

config store의 snap.areas를 기반으로 reactive targets. selectedId 추적. updateTarget(id, patch)로 양방향 동기화 (store → 자동 저장).

- [ ] **Step 2: SnapEditor.vue** — 3패널 레이아웃

좌측: 타겟 목록 (선택시 하이라이트). 중앙: vue-konva `<v-stage>` (모니터 비율 캔버스, `<v-rect>` 드래그 박스). 우측: PropertyPanel. 상단: 프리셋 USelectMenu. 하단 탭: Areas / Sector Mapping / Chains.

- [ ] **Step 3: PropertyPanel.vue** — 속성 폼 (양방향 동기화)

Type 토글: Area ↔ Action. Area시 x_ratio/y_ratio/w_ratio/h_ratio UInputNumber (0~1). Name UInput. Action시 WindowAction USelect. 폼 입력 → store 갱신 → 캔버스 자동 리렌더.

- [ ] **Step 4: ChainEditor.vue** — horizontal/vertical 체인 순서 편집

드래그로 순서 변경 (snap pool 항목). 추가/제거 버튼.

- [ ] **Step 5: 빌드 + 커밋**

```bash
npm run build
git add -A
git commit -m "feat: Snap Editor 3패널 (vue-konva) + 속성 패널 + 체인 편집기"
```

---

## Self-Review

**1. Spec coverage:**
- ✅ Nuxt UI **v4** (not v3) → `@nuxt/ui/vite` + `@nuxt/ui/vue-plugin`
- ✅ Pinia 전역 config store → Task 2
- ✅ Zod 런타임 검증 (Tauri IPC 경계) → Task 2
- ✅ 사이드바 + 라우팅 → Task 1
- ✅ 5개 설정 페이지 → Task 3
- ✅ Snap Editor (vue-konva) → Task 4
- ✅ nuxt-ui 스킬 베스트프랙티스: UApp 래퍼, semantic colors, `class="isolate"`

**2. Nuxt UI v4 핵심 변경사항 반영:**
- `@tailwindcss/vite` 불필요 — `@nuxt/ui/vite`가 통합
- `UButtonGroup` → `UFieldGroup` (v4에서 이름 변경)
- semantic colors: `text-default`, `bg-elevated`, `border-default` (raw palette 금지)
- UForm + Zod Standard Schema 통합 (v4 forms 가이드라인)
